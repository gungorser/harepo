template:
  - sensor:
      - &sensor_base
        unit_of_measurement: "°C"
        device_class: temperature
        state: "{{ state_attr(climate_entity, 'current_temperature') }}"
        availability: "{{ states(climate_entity) != 'unavailable' }}"

      - <<: *sensor_base
        name: sercan_thermostat
        unique_id: sercan_thermostat
        variables:
          climate_entity: "climate.sercan_climate"

      - <<: *sensor_base
        name: melike_thermostat
        unique_id: melike_thermostat
        variables:
          climate_entity: "climate.melike_climate"

      - <<: *sensor_base
        name: yatak_thermostat
        unique_id: yatak_thermostat
        variables:
          climate_entity: "climate.yatak_climate"

      - <<: *sensor_base
        name: misafir_thermostat
        unique_id: misafir_thermostat
        variables:
          climate_entity: "climate.misafir_climate"

      - <<: *sensor_base
        name: bebek_thermostat
        unique_id: bebek_thermostat
        variables:
          climate_entity: "climate.bebek_climate"
          
      - <<: *sensor_base
        name: banyo_thermostat
        unique_id: banyo_thermostat
        variables:
          climate_entity: "climate.banyo_climate"
